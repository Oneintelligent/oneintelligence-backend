from django.apps import AppConfig

class OneintelligentaiConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app.oneintelligentai'  # <-- must match the Python import path
